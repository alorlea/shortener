version: '2'

services:
  shortener-service:
    extends:
      file: common.yml
      service: shortener-service

  shortener-frontend:
    extends:
      file: common.yml
      service: shortener-frontend

  cassandra:
    extends:
      file: common.yml
      service: cassandra
